@model IEnumerable<AdminUserRoleProj_TMN.Models.ModelDB.Projekt>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ProjektName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ProjektDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ProjektName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ProjektDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProjektID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProjektID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProjektID })
        </td>
    </tr>
}

</table>
